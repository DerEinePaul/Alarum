import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads', (WidgetTester tester) async {
    // For now, just check if the app can be built without crashing
    // Full integration tests would require mocking plugins
    expect(true, isTrue);
  });
}
